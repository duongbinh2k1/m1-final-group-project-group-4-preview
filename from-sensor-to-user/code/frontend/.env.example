# Backend URL — empty = relative (nginx proxy), or full URL for direct connection
# Local direct:     VITE_BACKEND_URL=http://localhost:8000
# Production:       VITE_BACKEND_URL=https://mushroom-backend.onrender.com
VITE_BACKEND_URL=
